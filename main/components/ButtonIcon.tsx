import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

interface ButtonIconProps {
    icons: 'file' | 'camera';
}

const ButtonIcon = (icons: ButtonIconProps) => {
    return (
        <View>
            {/* {
                icons == 'file' ?

            } */}
        </View>
    );
};

export default ButtonIcon;

const styles = StyleSheet.create({});
